
/* iuap u-input */
import {CreatApp} from '../base'
function createDropdown (params, componentInfo) {
  let umeta = {'type': 'string'}
  CreatApp(componentInfo, umeta, params)
}
export default createDropdown
